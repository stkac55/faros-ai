# Default values for aion.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
global:
  faros:
    nodeJs:
      GCLoggingEnabled: false
  datadog:
    url: "https://api.datadoghq.com" # See https://docs.datadoghq.com/getting_started/site/#access-the-datadog-site
  postgresql:
    service:
      ports:
        postgresql: "5432"

replicaCount: 1

image:
  repository: farosai/aion
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "7066df5d4b62882992e35d2205cdc3485937d7be"

imagePullSecrets:
- name: dockerhub
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  capabilities:
    drop:
    - ALL
  readOnlyRootFilesystem: false
  runAsNonRoot: true
  runAsUser: 1000

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
  - host: aion.local
    paths:
    - path: /
      pathType: ImplementationSpecific
  tls: []

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

databaseSSLMode: "no-verify"
metabaseStatementTimeout: 55000
reportDatabaseSSLMode: "no-verify"
reportsRunFrequency: "8 hours"
reportsRunTimeout: "8 hours"
reportsStatementTimeout: "180 min"

dbName: &dbName aion-db
aionDb:
  enabled: true # When set to false, values for externalDb are used and must be set
  nameOverride: *dbName
  image:
    tag: "14.7.0" # Postgres DB version
  auth:
    enablePostgresUser: true   # Creates "postgres" admin user
    postgresPassword: admin
    username: aion
    password: aion
    database: aiondb
  primary:
    podSecurityContext:
      enabled: false
    containerSecurityContext:
      enabled: false
  volumePermissions:
    enabled: true
  tls:
    enabled: true
    autoGenerated: true

externalDb:
  host:
  port: 5432
  username:
  password:
  database:
